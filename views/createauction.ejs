<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إنشاء مزاد جديد - منصة مزادات</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Tajawal', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
        }
        .gradient-bg {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        }
        .form-container {
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }
        .input-group {
            position: relative;
            margin-bottom: 1.5rem;
        }
        .input-group i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6b7280;
        }
        .input-group input, .input-group textarea, .input-group select {
            padding-right: 15px;
            padding-left: 45px;
        }
        .image-upload-container {
            border: 2px dashed #d1d5db;
            border-radius: 12px;
            padding: 30px;
            text-align: center;
            transition: all 0.3s ease;
            background: #f9fafb;
        }
        .image-upload-container:hover {
            border-color: #3b82f6;
            background: #f0f9ff;
        }
        .image-preview {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
            gap: 10px;
            margin-top: 20px;
        }
        .preview-item {
            position: relative;
            border-radius: 8px;
            overflow: hidden;
            height: 120px;
        }
        .preview-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .remove-image {
            position: absolute;
            top: 5px;
            left: 5px;
            background: rgba(239, 68, 68, 0.8);
            color: white;
            border: none;
            border-radius: 50%;
            width: 24px;
            height: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 12px;
        }
        .step-indicator {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            position: relative;
        }
        .step-indicator::before {
            content: '';
            position: absolute;
            top: 15px;
            right: 0;
            left: 0;
            height: 2px;
            background: #e5e7eb;
            z-index: 1;
        }
        .step {
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            z-index: 2;
        }
        .step-number {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: #e5e7eb;
            color: #6b7280;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            margin-bottom: 8px;
            transition: all 0.3s ease;
        }
        .step.active .step-number {
            background: #3b82f6;
            color: white;
        }
        .step.completed .step-number {
            background: #10b981;
            color: white;
        }
        .step-label {
            font-size: 0.875rem;
            color: #6b7280;
            font-weight: 500;
        }
        .step.active .step-label {
            color: #3b82f6;
        }
        .step-content {
            display: none;
        }
        .step-content.active {
            display: block;
        }
        .price-input {
            position: relative;
        }
        .price-input:before {
            content: "ر.س";
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6b7280;
            font-weight: 500;
        }
        .datetime-input {
            position: relative;
        }
        .datetime-input i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6b7280;
        }
        .preview-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            overflow: hidden;
        }
    </style>
</head>
<body class="gradient-bg">
    <!-- شريط التنقل -->
    <%- include('partials/navbar') %>

    <!-- محتوى صفحة إنشاء مزاد -->
    <main class="max-w-4xl mx-auto px-4 py-8">
        <div class="form-container p-8">
            <div class="text-center mb-8">
                <h1 class="text-3xl font-bold text-gray-800">إنشاء مزاد جديد</h1>
                <p class="text-gray-600 mt-2">أضف معلومات مزادك وابدأ بيعه في منصتنا</p>
            </div>

            <!-- مؤشر الخطوات -->
            <div class="step-indicator">
                <div class="step active" data-step="1">
                    <div class="step-number">1</div>
                    <span class="step-label">معلومات أساسية</span>
                </div>
                <div class="step" data-step="2">
                    <div class="step-number">2</div>
                    <span class="step-label">الصور</span>
                </div>
                <div class="step" data-step="3">
                    <div class="step-number">3</div>
                    <span class="step-label">التوقيت</span>
                </div>
                <div class="step" data-step="4">
                    <div class="step-number">4</div>
                    <span class="step-label">مراجعة</span>
                </div>
            </div>

            <form id="createAuctionForm" action="/auctions" method="POST" enctype="multipart/form-data">
                <!-- الخطوة 1: المعلومات الأساسية -->
                <div class="step-content active" data-step="1">
                    <h2 class="text-2xl font-bold text-gray-800 mb-6">المعلومات الأساسية</h2>
                    
                    <div class="input-group">
                        <i class="fas fa-heading"></i>
                        <input id="title" name="title" type="text" required 
                               class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10"
                               placeholder="عنوان المزاد">
                    </div>

                    <div class="input-group">
                        <i class="fas fa-align-left"></i>
                        <textarea id="description" name="description" rows="5" required 
                                  class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10"
                                  placeholder="وصف مفصل للمنتج"></textarea>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div class="input-group">
                            <i class="fas fa-tag"></i>
                            <select id="category" name="categoryName" required 
                                    class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 bg-white">
                                <option value="" disabled selected>اختر الفئة</option>
                                <option value="cars">سيارات</option>
                                <option value="realestate">عقارات</option>
                                <option value="electronics">إلكترونيات</option>
                                <option value="jewelry">مجوهرات</option>
                                <option value="art">فنون</option>
                                <option value="furniture">أثاث</option>
                                <option value="other">أخرى</option>
                            </select>
                        </div>

                        <div class="input-group price-input">
                            <input id="start_price" name="start_price" type="number" step="0.01" min="0" required 
                                   class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 pl-12"
                                   placeholder="السعر الابتدائي">
                        </div>
                    </div>

                    <div class="flex justify-between mt-8">
                        <button type="button" class="next-step bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-6 rounded-lg transition duration-300">
                            التالي <i class="fas fa-arrow-left mr-2"></i>
                        </button>
                    </div>
                </div>

                <!-- الخطوة 2: رفع الصور -->
                <div class="step-content" data-step="2">
                    <h2 class="text-2xl font-bold text-gray-800 mb-6">رفع صور المنتج</h2>
                    
                    <div class="image-upload-container">
                        <i class="fas fa-cloud-upload-alt text-4xl text-gray-400 mb-4"></i>
                        <h3 class="text-lg font-medium text-gray-700 mb-2">اسحب وأفلت الصور هنا</h3>
                        <p class="text-gray-500 mb-4">أو انقر لاختيار الملفات</p>
                        <input type="file" id="imageUpload" name="image" multiple accept="image/*" class="hidden">
                        <button type="button" id="uploadTrigger" class="bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-6 rounded-lg transition duration-300">
                            اختيار الصور
                        </button>
                        <p class="text-sm text-gray-500 mt-4">يمكنك رفع حتى 10 صور (بحد أقصى 5MB لكل صورة)</p>
                    </div>

                    <div class="image-preview" id="imagePreview">
                        <!-- سيتم إضافة معاينات الصور هنا عبر JavaScript -->
                    </div>

                    <div class="flex justify-between mt-8">
                        <button type="button" class="prev-step bg-gray-300 hover:bg-gray-400 text-gray-800 font-medium py-3 px-6 rounded-lg transition duration-300">
                            <i class="fas fa-arrow-right ml-2"></i> السابق
                        </button>
                        <button type="button" class="next-step bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-6 rounded-lg transition duration-300">
                            التالي <i class="fas fa-arrow-left mr-2"></i>
                        </button>
                    </div>
                </div>

                <!-- الخطوة 3: توقيت المزاد -->
                <div class="step-content" data-step="3">
                    <h2 class="text-2xl font-bold text-gray-800 mb-6">توقيت المزاد</h2>
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div class="input-group datetime-input">
                            <i class="fas fa-play-circle"></i>
                            <input id="start_time" name="start_time" type="datetime-local" required 
                                   class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10">
                        </div>

                        <div class="input-group datetime-input">
                            <i class="fas fa-stop-circle"></i>
                            <input id="end_time" name="end_time" type="datetime-local" required 
                                   class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10">
                        </div>
                    </div>

                    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mt-6">
                        <div class="flex items-start">
                            <i class="fas fa-info-circle text-blue-500 mt-1 ml-2"></i>
                            <div>
                                <h4 class="font-medium text-blue-800">نصائح لاختيار التوقيت المناسب</h4>
                                <ul class="text-blue-700 text-sm mt-2 space-y-1">
                                    <li>• اختر أوقات الذروة (مساءً) لزيادة عدد المزايدين</li>
                                    <li>• مدة 3-7 أيام مناسبة لمعظم المنتجات</li>
                                    <li>• تجنب إنهاء المزاد في أيام العطلات الرسمية</li>
                                </ul>
                            </div>
                        </div>
                    </div>

                    <div class="flex justify-between mt-8">
                        <button type="button" class="prev-step bg-gray-300 hover:bg-gray-400 text-gray-800 font-medium py-3 px-6 rounded-lg transition duration-300">
                            <i class="fas fa-arrow-right ml-2"></i> السابق
                        </button>
                        <button type="button" class="next-step bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-6 rounded-lg transition duration-300">
                            التالي <i class="fas fa-arrow-left mr-2"></i>
                        </button>
                    </div>
                </div>

                <!-- الخطوة 4: المراجعة النهائية -->
                <div class="step-content" data-step="4">
                    <h2 class="text-2xl font-bold text-gray-800 mb-6">مراجعة المعلومات</h2>
                    
                    <div class="preview-card p-6 mb-6">
                        <h3 class="text-xl font-bold text-gray-800 mb-4">معاينة المزاد</h3>
                        
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <div>
                                <h4 class="font-medium text-gray-700 mb-2">العنوان:</h4>
                                <p id="preview-title" class="text-gray-900 mb-4">-</p>
                                
                                <h4 class="font-medium text-gray-700 mb-2">الوصف:</h4>
                                <p id="preview-description" class="text-gray-900 mb-4">-</p>
                                
                                <h4 class="font-medium text-gray-700 mb-2">الفئة:</h4>
                                <p id="preview-category" class="text-gray-900">-</p>
                            </div>
                            
                            <div>
                                <h4 class="font-medium text-gray-700 mb-2">السعر الابتدائي:</h4>
                                <p id="preview-price" class="text-gray-900 mb-4">-</p>
                                
                                <h4 class="font-medium text-gray-700 mb-2">وقت البدء:</h4>
                                <p id="preview-start" class="text-gray-900 mb-4">-</p>
                                
                                <h4 class="font-medium text-gray-700 mb-2">وقت الانتهاء:</h4>
                                <p id="preview-end" class="text-gray-900">-</p>
                            </div>
                        </div>
                        
                        <div class="mt-6">
                            <h4 class="font-medium text-gray-700 mb-2">الصور:</h4>
                            <div id="preview-images" class="flex flex-wrap gap-2">
                                <p class="text-gray-500">لم يتم رفع أي صور</p>
                            </div>
                        </div>
                    </div>

                    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
                        <div class="flex items-start">
                            <i class="fas fa-exclamation-triangle text-yellow-500 mt-1 ml-2"></i>
                            <div>
                                <h4 class="font-medium text-yellow-800">تنبيه مهم</h4>
                                <p class="text-yellow-700 text-sm mt-1">بعد نشر المزاد، لا يمكنك تعديل المعلومات الأساسية. تأكد من صحة جميع البيانات قبل المتابعة.</p>
                            </div>
                        </div>
                    </div>

                    <div class="flex justify-between mt-8">
                        <button type="button" class="prev-step bg-gray-300 hover:bg-gray-400 text-gray-800 font-medium py-3 px-6 rounded-lg transition duration-300">
                            <i class="fas fa-arrow-right ml-2"></i> السابق
                        </button>
                        <button type="submit" class="bg-green-600 hover:bg-green-700 text-white font-medium py-3 px-6 rounded-lg transition duration-300">
                            <i class="fas fa-check ml-2"></i> نشر المزاد
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </main>

    <!-- تذييل الصفحة -->
    <%- include('partials/footer') %>

    <script>
        // متغيرات لتخزين البيانات
        let uploadedImages = [];
        let currentStep = 1;

        // عناصر DOM
        const steps = document.querySelectorAll('.step');
        const stepContents = document.querySelectorAll('.step-content');
        const nextButtons = document.querySelectorAll('.next-step');
        const prevButtons = document.querySelectorAll('.prev-step');
        const form = document.getElementById('createAuctionForm');
        const imageUpload = document.getElementById('imageUpload');
        const uploadTrigger = document.getElementById('uploadTrigger');
        const imagePreview = document.getElementById('imagePreview');

        // الانتقال بين الخطوات
        nextButtons.forEach(button => {
            button.addEventListener('click', () => {
                if (validateStep(currentStep)) {
                    currentStep++;
                    updateSteps();
                }
            });
        });

        prevButtons.forEach(button => {
            button.addEventListener('click', () => {
                currentStep--;
                updateSteps();
            });
        });

        // تحديث عرض الخطوات
        function updateSteps() {
            // تحديث مؤشر الخطوات
            steps.forEach(step => {
                const stepNum = parseInt(step.getAttribute('data-step'));
                step.classList.remove('active', 'completed');
                
                if (stepNum === currentStep) {
                    step.classList.add('active');
                } else if (stepNum < currentStep) {
                    step.classList.add('completed');
                }
            });

            // تحديث محتوى الخطوات
            stepContents.forEach(content => {
                const contentStep = parseInt(content.getAttribute('data-step'));
                content.classList.remove('active');
                
                if (contentStep === currentStep) {
                    content.classList.add('active');
                }
            });

            // إذا كنا في الخطوة الرابعة، تحديث المعاينة
            if (currentStep === 4) {
                updatePreview();
            }
        }

        // التحقق من صحة البيانات في كل خطوة
        function validateStep(step) {
            switch (step) {
                case 1:
                    const title = document.getElementById('title').value;
                    const description = document.getElementById('description').value;
                    const category = document.getElementById('category').value;
                    const startPrice = document.getElementById('start_price').value;
                    
                    if (!title || !description || !category || !startPrice) {
                        alert('يرجى ملء جميع الحقول المطلوبة في هذه الخطوة');
                        return false;
                    }
                    return true;
                    
                case 2:
                    if (uploadedImages.length === 0) {
                        alert('يرجى رفع صورة واحدة على الأقل للمنتج');
                        return false;
                    }
                    return true;
                    
                case 3:
                    const startTime = document.getElementById('start_time').value;
                    const endTime = document.getElementById('end_time').value;
                    
                    if (!startTime || !endTime) {
                        alert('يرجى تحديد وقت البدء والانتهاء للمزاد');
                        return false;
                    }
                    
                    const startDate = new Date(startTime);
                    const endDate = new Date(endTime);
                    const now = new Date();
                    
                    if (startDate <= now) {
                        alert('وقت البدء يجب أن يكون في المستقبل');
                        return false;
                    }
                    
                    if (endDate <= startDate) {
                        alert('وقت الانتهاء يجب أن يكون بعد وقت البدء');
                        return false;
                    }
                    
                    return true;
                    
                default:
                    return true;
            }
        }

        // تحديث المعاينة في الخطوة الرابعة
        function updatePreview() {
            document.getElementById('preview-title').textContent = document.getElementById('title').value;
            document.getElementById('preview-description').textContent = document.getElementById('description').value;
            document.getElementById('preview-category').textContent = document.getElementById('category').options[document.getElementById('category').selectedIndex].text;
            document.getElementById('preview-price').textContent = document.getElementById('start_price').value + ' ر.س';
            
            const startTime = document.getElementById('start_time').value;
            const endTime = document.getElementById('end_time').value;
            
            if (startTime) {
                document.getElementById('preview-start').textContent = new Date(startTime).toLocaleString('ar-SA');
            }
            
            if (endTime) {
                document.getElementById('preview-end').textContent = new Date(endTime).toLocaleString('ar-SA');
            }
            
            // تحديث معاينة الصور
            const previewImagesContainer = document.getElementById('preview-images');
            previewImagesContainer.innerHTML = '';
            
            if (uploadedImages.length > 0) {
                uploadedImages.forEach((image, index) => {
                    const imgElement = document.createElement('div');
                    imgElement.className = 'preview-item';
                    imgElement.innerHTML = `
                        <img src="${image.preview}" alt="معاينة ${index + 1}">
                    `;
                    previewImagesContainer.appendChild(imgElement);
                });
            } else {
                previewImagesContainer.innerHTML = '<p class="text-gray-500">لم يتم رفع أي صور</p>';
            }
        }

        // التعامل مع رفع الصور
        uploadTrigger.addEventListener('click', () => {
            imageUpload.click();
        });

        imageUpload.addEventListener('change', (e) => {
            const files = e.target.files;
            
            if (files.length + uploadedImages.length > 10) {
                alert('يمكنك رفع 10 صور كحد أقصى');
                return;
            }
            
            for (let i = 0; i < files.length; i++) {
                const file = files[i];
                
                if (file.size > 5 * 1024 * 1024) {
                    alert(`الصورة ${file.name} تتجاوز الحجم المسموح به (5MB)`);
                    continue;
                }
                
                const reader = new FileReader();
                
                reader.onload = (e) => {
                    const imageData = {
                        file: file,
                        preview: e.target.result
                    };
                    
                    uploadedImages.push(imageData);
                    updateImagePreview();
                };
                
                reader.readAsDataURL(file);
            }
            
            // إعادة تعيين حقل الرفع للسماح برفع نفس الصورة مرة أخرى إذا لزم الأمر
            imageUpload.value = '';
        });

        // تحديث معاينة الصور
        function updateImagePreview() {
            imagePreview.innerHTML = '';
            
            uploadedImages.forEach((image, index) => {
                const previewItem = document.createElement('div');
                previewItem.className = 'preview-item';
                previewItem.innerHTML = `
                    <img src="${image.preview}" alt="معاينة ${index + 1}">
                    <button type="button" class="remove-image" data-index="${index}">
                        <i class="fas fa-times"></i>
                    </button>
                `;
                imagePreview.appendChild(previewItem);
            });
            
            // إضافة مستمعي الأحداث لأزرار الحذف
            document.querySelectorAll('.remove-image').forEach(button => {
                button.addEventListener('click', (e) => {
                    const index = parseInt(e.target.closest('.remove-image').getAttribute('data-index'));
                    uploadedImages.splice(index, 1);
                    updateImagePreview();
                });
            });
        }

        // إرسال النموذج
        form.addEventListener('submit', (e) => {
            e.preventDefault();
            
            if (validateStep(4)) {
                // هنا يمكنك إضافة كود لإرسال البيانات إلى الخادم
                // alert('تم إنشاء المزاد بنجاح! سيتم مراجعته ونشره قريباً.');
                form.submit(); // إلغاء التعليق عند ربطها بالخادم
            }
        });

        // تعيين القيم الافتراضية للتواريخ
        document.addEventListener('DOMContentLoaded', () => {
            const now = new Date();
            const startTime = new Date(now.getTime() + 60 * 60 * 1000); // بعد ساعة من الآن
            const endTime = new Date(startTime.getTime() + 7 * 24 * 60 * 60 * 1000); // بعد 7 أيام من وقت البدء
            
            document.getElementById('start_time').value = formatDateTime(startTime);
            document.getElementById('end_time').value = formatDateTime(endTime);
        });

        // تنسيق التاريخ والوقت لحقل الإدخال
        function formatDateTime(date) {
            const year = date.getFullYear();
            const month = String(date.getMonth() + 1).padStart(2, '0');
            const day = String(date.getDate()).padStart(2, '0');
            const hours = String(date.getHours()).padStart(2, '0');
            const minutes = String(date.getMinutes()).padStart(2, '0');
            
            return `${year}-${month}-${day}T${hours}:${minutes}`;
        }
    </script>
</body>
</html>