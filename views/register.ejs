<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تسجيل جديد - منصة مزادات</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Tajawal', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .gradient-bg {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        }
        .form-container {
            background: white;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }
        .input-group {
            position: relative;
            margin-bottom: 1.5rem;
        }
        .input-group i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6b7280;
        }
        .input-group input {
            padding-right: 15px;
            padding-left: 45px;
        }
        .password-toggle {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #6b7280;
        }
    </style>
</head>
<body class="gradient-bg">
    <%- include('partials/navbar') %>
     <!-- محتوى صفحة التسجيل -->
    <main class="flex-grow flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full form-container p-8">
            <div class="text-center mb-8">
                <h2 class="text-3xl font-bold text-gray-800">إنشاء حساب جديد</h2>
                <p class="text-gray-600 mt-2">انضم إلى منصة المزادات الرائدة</p>
            </div>

            <form class="mt-8 space-y-6" action="/users/register" method="POST">
                <!-- الاسم الكامل -->
                <div class="input-group">
                    <i class="fas fa-user"></i>
                    <input id="full-name" name="full_name" type="text" required 
                           class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10"
                           placeholder="الاسم الكامل">
                </div>

                <!-- البريد الإلكتروني -->
                <div class="input-group">
                    <i class="fas fa-envelope"></i>
                    <input id="email-address" name="email" type="email" autocomplete="email" required 
                           class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10"
                           placeholder="البريد الإلكتروني">
                </div>

                <!-- العنوان -->
                <div class="input-group">
                    <i class="fas fa-map-marker-alt"></i>
                    <input id="address" name="address" type="text" required 
                           class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10"
                           placeholder="العنوان">
                </div>

                <!-- رقم الهاتف -->
                <div class="input-group">
                    <i class="fas fa-phone"></i>
                    <input id="phone" name="phone" type="tel" required 
                           class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10"
                           placeholder="رقم الهاتف">
                </div>

                <!-- نوع المستخدم -->
<div class="input-group">
    <i class="fas fa-user-tag"></i>
    <select id="user-type" name="role" required
        class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 bg-white">
        <option value="" disabled selected>اختر نوع المستخدم</option>
        <option value="buyer">مشتري</option>
        <option value="seller">بائع</option>
    </select>
</div>


                <!-- كلمة المرور -->
                <div class="input-group">
                    <i class="fas fa-lock"></i>
                    <input id="password" name="password" type="password" autocomplete="new-password" required 
                           class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10"
                           placeholder="كلمة المرور">
                    <span class="password-toggle" onclick="togglePassword('password')">
                        <i class="far fa-eye"></i>
                    </span>
                </div>

                <!-- تأكيد كلمة المرور -->
                <div class="input-group">
                    <i class="fas fa-lock"></i>
                    <input id="confirm-password" name="confirm_password" type="password" autocomplete="new-password" required 
                           class="appearance-none rounded-lg relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10"
                           placeholder="تأكيد كلمة المرور">
                    <span class="password-toggle" onclick="togglePassword('confirm-password')">
                        <i class="far fa-eye"></i>
                    </span>
                </div>

                <!-- شروط الخدمة -->
                <div class="flex items-center">
                    <input id="agree-terms" name="agreeTerms" type="checkbox" 
                           class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded ml-2">
                    <label for="agree-terms" class="text-sm text-gray-700">
                        أوافق على <a href="#" class="font-medium text-blue-600 hover:text-blue-500">شروط الخدمة</a> و <a href="#" class="font-medium text-blue-600 hover:text-blue-500">سياسة الخصوصية</a>
                    </label>
                </div>

                <!-- زر التسجيل -->
                <div>
                    <button type="submit" 
                            class="group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-medium rounded-lg text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition duration-300">
                        إنشاء حساب
                    </button>
                </div>

                <!-- رابط تسجيل الدخول -->
                <div class="text-center">
                    <span class="text-gray-600">لديك حساب بالفعل؟</span>
                    <a href="/login" class="font-medium text-blue-600 hover:text-blue-500 mr-1">سجل الدخول هنا</a>
                </div>
            </form>
        </div>
    </main>

    <!-- تضمين الفوتر -->
    <%- include('partials/footer') %>
     <!-- JavaScript للقائمة المنسدلة في الجوّال -->
    <script>
      

        // وظيفة إظهار/إخفاء كلمة المرور
        function togglePassword(fieldId) {
            const passwordField = document.getElementById(fieldId);
            const icon = passwordField.nextElementSibling.querySelector('i');
            
            if (passwordField.type === "password") {
                passwordField.type = "text";
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                passwordField.type = "password";
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        }

        // التحقق من تطابق كلمتي المرور والموافقة على الشروط قبل الإرسال
        document.querySelector('form').addEventListener('submit', function(e) {
    const password = document.getElementById('password').value;
    const confirmPassword = document.getElementById('confirm-password').value;
    const agreeTerms = document.getElementById('agree-terms').checked;

    // تحقق من تطابق كلمتي المرور
    if (password !== confirmPassword) {
        e.preventDefault(); // منع الإرسال فقط عند الخطأ
        alert('كلمتا المرور غير متطابقتين');
        return;
    }

    // تحقق من الموافقة على الشروط
    if (!agreeTerms) {
        e.preventDefault(); // منع الإرسال فقط عند الخطأ
        alert('يجب الموافقة على شروط الخدمة');
        return;
    }

    // إذا كل شيء صحيح، الفورم سيُرسل تلقائياً إلى /users/register
});

    
    </script>
</body>
</html>